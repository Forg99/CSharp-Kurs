U 15 Logical error 1:
the if loop is checking if any given number in the array is smaller than "max" 
solution:
make the if loop check if any given number in the array is grater than "max"

U21 Logical erro 2:
the if was in acending order which effectively only checked if the user is over 18 and there was no prompt enter the users age

U22 Logical error 3:
for (int i = 0; i <= numbers.Length; i++) tries to run the loop one too many times